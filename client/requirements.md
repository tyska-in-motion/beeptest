## Packages
lucide-react | Icons for UI elements
react-hook-form | Form state management
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form
clsx | Class name utility
tailwind-merge | Class name utility
framer-motion | Smooth animations for timer and transitions

## Notes
Using Web Audio API for beeps (no external dependency needed).
Using window.speechSynthesis for TTS (no external dependency needed).
Ensure "pl-PL" voice selection logic is robust.
